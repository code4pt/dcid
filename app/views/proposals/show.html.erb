<% provide(:title, @proposal.title) %>

<div class="row">
  <div class="span12">
    <h2><%= @proposal.title %></h2>
    <br>
    <div class="row">
      <div class="span9">
        <p><%= @proposal.problem %></p>
        <br>
        <h3>Solução</h3>
        <p><%= @proposal.solution %></p>
        <br class="visible-phone"> <!-- separates text -->
      </div>
      <div class="span3 text-center">
        <p><%= render 'shared/vote_buttons', :proposal => @proposal %></p>
        <p><%= link_to @proposal.user.short_name, @proposal.user %> em <%= @proposal.created_at.to_time.strftime('%e/%m/%Y') %></p>
      </div>
    </div>
  </div>
</div>
