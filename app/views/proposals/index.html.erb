<% provide(:title, 'Propostas') %>

<div class="row">
  <div class="col-md-12">

    <h1>Todas as propostas</h1>

    <p><%= link_to '<button class="btn btn-default btn-success pull-right">Criar proposta</button>'.html_safe, new_proposal_path %></p>
    <br><br><br>

    <% if @proposals.empty? %>
        <p class="text-center">Não foram encontradas propostas.</p>
      <% else %>
        <ul>
          <%= render @proposals %>
        </ul>
    <% end %>

  </div>
</div>

<div class="row">
  <div class="col-md-12 text-center">
    <%= will_paginate @collection, renderer: BootstrapPagination::Rails %>
  </div>
</div>
