<% provide(:title, 'Participar') %>

<div class="row">
  <div class="span12">

    <h1>Participe</h1>

    <div class="row">
      <div class="span6 offset3">
        <%= form_for(@user) do |f| %>
          <%= render 'shared/error_messages', object: f.object %>

          <%= f.label :name, "Nome" %>
          <%= f.text_field :name, placeholder: "Primeiro e último nome" %>

          <%= f.label :email, "Email" %>
          <%= f.text_field :email %>

          <%= f.label :password, "Palavra-chave" %>
          <%= f.password_field :password %>

          <%= f.label :password_confirmation, "Repita a palavra-chave" %>
          <%= f.password_field :password_confirmation, placeholder: "Só para confirmar" %>

          <%= f.label :citizen_number, 'Número do BI/CC&nbsp;<small id="bicc" class="visible-desktop pull-right" data-toggle="tooltip" data-placement="left" title="O número de Bilhete de Identidade (BI) ou Cartão de Cidadão (CC) garante que cada cidadão vota uma vez. Deve introduzir também os dígitos de controlo presentes no cartão, que se encontram à direita do número do BI/CC.">Que números?</small>'.html_safe %>
          <%= f.text_field :citizen_number, class: "inline input-medium " %>
          <%= f.text_field :control_numbers, placeholder: "Controlo", class: "inline input-small" %>

          <br>
          <%= f.submit "Criar perfil", class: "btn btn-success" %>
        <% end %>
      </div>
    </div>

    <small class="muted hidden-desktop">Nota: O número de Bilhete de Identidade (BI) ou Cartão de Cidadão (CC) garante que cada cidadão vota uma vez. Deve introduzir também os dígitos de controlo presentes no cartão, que se encontram à direita do número do BI/CC.</small>
  </div>
</div>


<% content_for :head do %>
  <script type="text/javascript">
    $(function() {$('#bicc').tooltip();});
  </script>
<% end %>
