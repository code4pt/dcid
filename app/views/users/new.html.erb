<% provide(:title, 'Participar') %>

<div class="row">
  <div class="col-md-12">
    <h1>Participe</h1>

    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <%= form_for(@user) do |f| %>
          <%= render 'shared/error_messages', object: f.object %>

          <%= f.label :name, "Nome" %>
          <%= f.text_field :name, placeholder: "Primeiro e último nome" %>

          <%= f.label :email, "Email" %>
          <%= f.text_field :email %>

          <%= f.label :citizen_number, 'Número do BI/CC&nbsp;<small id="bicc" class="visible-md visible-lg pull-right" data-toggle="tooltip" data-placement="left" title="O número de Bilhete de Identidade (BI) ou Cartão de Cidadão (CC) garante que cada cidadão vota uma vez. Não podemos usar o email porque o mesmo cidadão podia criar dois perfis com dois emails diferentes e votar a dobrar - o que seria injusto.">Porquê?</small>'.html_safe %>
          <%= f.text_field :citizen_number %>

          <%= f.label :password, "Palavra-chave" %>
          <%= f.password_field :password %>

          <%= f.label :password_confirmation, "Repita a palavra-chave" %>
          <%= f.password_field :password_confirmation, placeholder: "Só para confirmar" %>

          <br>
          <%= f.submit "Criar perfil", class: "btn btn-default btn-success" %>
        <% end %>
      </div>
    </div>

    <small class="text-muted hidden-md hidden-lg">Nota: O número de Bilhete de Identidade (BI) ou Cartão de Cidadão (CC) garante que cada cidadão vota uma vez. Não podemos usar o email porque o mesmo cidadão podia criar dois perfis com dois emails diferentes e votar a dobrar - o que seria injusto.</small>
  </div>
</div>


<% content_for :head do %>
  <script type="text/javascript">
    $(function() {$('#bicc').tooltip();});
  </script>
<% end %>
