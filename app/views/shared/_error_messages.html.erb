<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      Há <%= pluralize(object.errors.count, "problema") %> com o seu preenchimento.
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
