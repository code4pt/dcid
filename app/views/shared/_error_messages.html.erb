<% if @user.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      Há <%= pluralize(@user.errors.count, "problema") %> com o seu formulário.
    </div>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>